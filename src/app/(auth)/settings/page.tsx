import Page from '@/shared/ui/page';
import { MainSettings } from '@/widgets/settings/ui/main-settings';

export default function SettingsPage() {
  return (
    <Page>
      <MainSettings />
    </Page>
  );
}
